import hmh.mms.Program

model {
	Program program
}

json g.render(program){

    version "correlationslist"
    //  Requested seperately when the Correlations tab is clicked on -  
    correlationSource tmpl.'/correlationslist/correlationSource'(program.correlations)
}